{
  "extra_tables" => {
    "all" => { descr = "Run some extra tables"; share = 0 };
    "none" => { descr = "Run no extra tables" };
  };
  "run_time_steps" => {
    "up_to_1min" => { descr = "Run only the 1min aggregate"; share = 1 };
    "up_to_10min" => { descr = "Run 1min and 10min aggregates" };
    "up_to_1hour" => { descr = "Run 1min, 10min and 1hour aggregates" };
  };
  "run_per_app_aad" => {
    "on" => { descr = "Run per application automatic anomaly detection"; share = 1 };
    "off" => { descr = "Does not run per application automatic anomaly detection" };
  };
}
